# CIC Document Extraction - Server Requirements
# For Linux server with limited RAM (2GB)
#
# Install with: pip install -r requirements-server.txt

# Core extraction
pdfplumber>=0.9.0
pytesseract>=0.3.10
pdf2image>=1.16.0

# Processing
tqdm>=4.65.0

# Evaluation (optional - can skip if just running extraction)
# pandas>=2.0.0
# openpyxl>=3.1.0

# System dependencies (install via apt):
# sudo apt-get update
# sudo apt-get install -y tesseract-ocr poppler-utils
